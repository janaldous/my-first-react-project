import *  as React from 'react';
import './shared.scss';

export const LoadingOverlay: React.FunctionComponent<{}> = () => {
    return (
        <div className="loading-overlay-container"></div>
    );
}